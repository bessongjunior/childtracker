
// const handleLogout = () => {

//     const Logout = (data) => {
//         return axios.post(`127.0.0.1:5000/users/v1/logout`, data, {
//           headers: { Authorization: `Bearer ${token}` },
//         });
//       };
//     localStorage.removeItem("user");
//     return navigate("/authentication/sign-in");
//   };
//   data == "user": user_exists.toJSON(), from backend!! update when authcontext in use!